# 点单系统 v1 设计文档

> 文档驱动开发：本文件先于代码，所有实现细节以本文档为准；后续改动先改文档再改代码。

## 1. 目标

做一个轻量级多人点单 H5 系统，主要场景是朋友/同事聚餐时各自扫码点单，最后汇总成一桌的清单。

- 多人点单：通过共享一个 **会话（Session）** 实现，每个人点单都归属到对应人名下。
- 不需要支付：只生成汇总清单。
- 移动端为主，PC 兼容：单列布局 + 自适应宽度，最大宽度 480px 居中。
- 技术栈：Python + Flask，单文件后端，HTML/CSS/JS 不引外部库。
- 数据存储：进程内字典（重启即清空，v1 不接数据库）。

## 2. 角色与流程

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 1.创建/  │ -> │ 2.进入   │ -> │ 3.选菜品 │ -> │ 4.加入   │
│ 加入会话 │    │ 点单页   │    │ 酱料/附加│    │ 本人购物车│
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                       │
                                                       v
                                                ┌──────────┐
                                                │ 5.所有人  │
                                                │ 点完查看  │
                                                │ 汇总清单  │
                                                └──────────┘
```

每个人进入会话后，先填昵称（仅本机会话内有效，用于区分谁点了什么），然后从菜品列表里选，重复操作直到结束；最后任何人可点 "查看汇总"。

## 3. 数据模型

```python
Sauce = {
    "id": "s_001",
    "name": "蒜蓉",
    "price": 0.0          # 酱料默认 0 元；为正数时叠加
}

AddOn = {
    "id": "a_001",
    "name": "加蛋",
    "price": 2.0
}

Dish = {
    "id": "d_001",
    "name": "牛肉面",
    "category": "主食",
    "price": 28.0,
    "description": "手擀面 + 牛腱",
    "sauce_ids": ["s_001", "s_002"],     # 可选酱料
    "sauce_mode": "single_required",     # 选填；见 §3.2
    "addon_ids": ["a_001", "a_002"]      # 可选附加品
}

OrderItem = {
    "id": "uuid",                          # 订单条目 ID
    "session_id": "xxx",
    "user_name": "小王",                   # 点单人昵称
    "user_no": 1,                          # 该会话内的用户序号，详见 §3.1
    "dish_id": "d_001",
    "sauce_ids": ["s_001"],
    "addon_ids": ["a_001"],
    "note": "少辣",                        # 备注，可空
    "unit_price": 30.0,                    # 菜品 + 酱料 + 附加品 计算后单价
    "created_at": "2026-06-03T12:00:00"
}

Session = {
    "id": "6位字母数字",                   # 短码，便于口头/扫码分享
    "title": "周五聚餐",                   # 标题，可空
    "created_at": "...",
    "status": "active" | "closed",
    "user_names": ["小王", "小李", ...],   # 按首次下单顺序记录，序号 = 列表下标 + 1
    "order_ids": [OrderItem.id, ...]
}
```

价格计算：`unit_price = dish.price + sum(sauce.price for s in sauces) + sum(addon.price for a in addons)`。

### 3.1 用户序号规则

- 同一会话内，每个**昵称**在该会话下首次下单时分配一个递增序号（1, 2, 3...）。
- 后续该昵称再下单，序号保持不变。
- 改昵称后再下单 → 视为新用户，分配下一个新序号。
- 序号在汇总页和后台页都展示，格式为 `1号 小王`。

### 3.2 酱料选择模式

`Dish.sauce_mode` 字段控制该菜品的酱料交互：

| 值 | 含义 | UI 行为 |
| --- | --- | --- |
| 缺省 / `"multi_optional"` | 多选或不选 | 原行为：可勾选 0..N 个 |
| `"single_required"` | 必选 1 个 | 单选；未选时"加入清单"按钮禁用 |

后端在创建订单时校验：模式为 `single_required` 时，`sauce_ids` 必须非空且数量为 1。

### 3.3 主食底（base）选项

部分菜品有"主食底"维度（如双拼碗可选杂粮饭/意面/荞麦面），用于：
- 在菜单页把这部分菜品当作"一道菜"展示，避免 3 倍行数
- 在汇总/后台展示成 `菜名（主食底）` 形式（如 `鸡胸肉牛肉双拼碗（杂粮饭）`）
- 底本身**不作为独立统计维度**——汇总时按 `dish + base` 整体聚合

数据模型：

```json
Dish = {
    "id": "d_chicken_beef",
    "name": "鸡胸肉牛肉双拼碗",
    "category": "双拼碗",
    "base_options": ["杂粮饭", "意面", "荞麦面"],  // 选填；存在则视作必选单选
    ...
}

OrderItem = {
    "dish_id": "d_chicken_beef",
    "base": "杂粮饭",  // 当 dish.base_options 存在时必填
    ...
}
```

行为约束：
- 后端校验：若 `dish.base_options` 存在，`base` 必须在该列表中且非空。
- UI：弹层在酱料之上加一个"主食底（X选1）"的必选单选组；任一必选项未选时确认按钮禁用。
- 展示名 = `dish.name + (base ? "（" + base + "）" : "")`，由前端根据 order 计算。

## 4. API 设计

所有响应 JSON，错误统一：`{"error": "msg"}`，HTTP 4xx/5xx。

| Method | Path | 说明 |
| --- | --- | --- |
| GET | `/` | 首页：创建会话 / 输入会话码加入 |
| POST | `/api/session` | 创建会话。body: `{title?}`，返回 `{id, title}` |
| GET | `/api/session/<id>` | 获取会话信息 + 全部订单 |
| POST | `/api/session/<id>/order` | 新增一条订单条目。body: `{user_name, dish_id, sauce_ids, addon_ids, note?}` |
| DELETE | `/api/order/<order_id>` | 删除一条订单条目（仅本人昵称匹配可删，v1 简化为只要会话 active 就能删） |
| GET | `/api/menu` | 返回完整菜单（菜品 + 酱料 + 附加品），用于点单页加载 |
| GET | `/menu/<session_id>` | 点单页 H5 |
| GET | `/summary/<session_id>` | 汇总页 H5（按人分组） |
| GET | `/admin/<session_id>` | 后台汇总页 H5（按菜品聚合） |

会话码生成：6 位 base32 字母数字（去除易混淆字符 0/O/1/I），全局唯一，冲突重试 5 次。

## 5. 页面设计

### 5.1 首页 `/`
- 顶部标题 "点单系统"
- 区块 A：填标题（可选）→ 「创建新会话」按钮
- 区块 B：填会话码 → 「加入会话」按钮
- 样式：居中卡片，最大宽 480px，背景 #f5f5f5

### 5.2 点单页 `/menu/<session_id>`
- 顶部：会话标题 + 会话码（大字可分享） + 「查看汇总」入口 + 「我的昵称」可改
- 菜品分类 tab（如：主食 / 小吃 / 饮品）
- 菜品卡片：名称、价格、描述、加入按钮
- 点 "加入" 弹出底部弹层：
  - 展示当前菜品
  - 酱料多选（单选/多选：v1 用 checkbox 多选，可选 0 个）
  - 附加品多选
  - 备注 textarea
  - "加入我的清单" 按钮 → 弹层关闭
- 底部固定栏：「已点 N 份 · 查看汇总」

### 5.3 汇总页 `/summary/<session_id>`
- 顶部：会话标题
- 按 `user_name` 分组列出所有订单条目，分组标题显示 `序号 + 昵称`（如 `1号 小王`）
- 每条显示：菜品、酱料、附加品、备注、单价
- 底部合计：总金额 + 数量
- 移动端单列；PC 居中，最大宽 720px

### 5.4 后台汇总页 `/admin/<session_id>`
- 顶部：会话标题 + 「按人汇总」入口（跳 `/summary`）
- 顶部 tab 切换两种视图：
  - **按菜品汇总**：相同菜品的订单合为一组，组内按规格（酱料+附加品+备注）再聚合
  - **按项目计数**：把每单里"1 道菜 + N 个酱料 + M 个附加品"展开成独立 SKU，分别计数
- 适用场景：服务员/厨房视角，看每样要备几份

#### 5.4.1 按菜品汇总
- 每行展示：`菜品名 × 份数` + 该规格下 `几号 谁` 点了
- 底部合计：总份数 + 总金额

#### 5.4.2 按项目计数
- 一行 = 一个独立物品（菜品 / 酱料 / 附加品各算一条）
- 标签：菜品、酱料、附加品
- 三列：
  | 项目 | 数量 | 序号 |
  | --- | --- | --- |
  | 红烧牛肉面 | 3 | 1号 2号 3号 |
  | 加蛋 | 2 | 1号 3号 |
  | 加肉 | 1 | 1号 |
- 排序：按数量降序
- 底部：合计项目数 + 数量总和
- 适用场景：备菜数蛋/数肉等具体物料统计

## 6. 样式规范

- 主色：`#ff6b35`（橘色，食欲系）
- 辅色：`#2c3e50`（文字）
- 背景：`#f5f5f5`
- 字体：`-apple-system, "PingFang SC", "Microsoft YaHei", sans-serif`
- 圆角：8px
- 移动端：单列、`100vw`、底部安全区预留 `env(safe-area-inset-bottom)`
- PC 端：内容居中，最大宽 480px（点单页）/ 720px（汇总页），背景灰色

## 7. 项目结构

```
order_v1/
├── DESIGN.md            # 本文件
├── README.md            # 启动说明
├── requirements.txt     # flask
├── app.py               # 后端单文件
├── data/
│   └── menu.json        # 初始菜单数据
├── templates/
│   ├── index.html       # 首页
│   ├── menu.html        # 点单页
│   ├── summary.html     # 汇总页（按人）
│   └── admin.html       # 后台汇总页（按菜）
└── static/
    ├── css/style.css
    └── js/
        ├── menu.js
        └── summary.js
```

## 8. 后续可改进（不在 v1 范围）

- 数据库持久化（SQLite/Postgres）
- 多人实时同步（WebSocket / SSE）
- 用户身份持久化（cookie/localStorage）
- 菜品图片、搜索
- 导出 PDF / 微信分享卡片
- 管理员后台维护菜单
- 菜品库存、限购
